const name = 'John Smith';

const age = 55;

const height = 175.5;

const favoriteColors = ['Red', 'Green', 'Blue'];

const user = {
    name,
    age,
    height,
    favoriteColors,
};

module.exports = {
    name,
    age,
    height,
    favoriteColors,
    user,
};
